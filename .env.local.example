# --- GROQ CONFIGURATION ---

# We use the variable name OPENAI_API_KEY, but we put the GROQ key inside.
# NOTE: do NOT commit real keys. Put your own Groq key here.
NEXT_PUBLIC_OPENAI_API_KEY=YOUR_GROQ_API_KEY

# This tells the app: "Don't go to OpenAI, go to Groq server"
NEXT_PUBLIC_OPENAI_BASE_URL=https://api.groq.com/openai/v1

# Models
# For Text/Enrichment (Fast and smart)
NEXT_PUBLIC_LLM_MODEL=llama-3.3-70b-versatile

# For Audio Transcription (Groq's Whisper is insanely fast)
NEXT_PUBLIC_WHISPER_MODEL=whisper-large-v3-turbo

# Language for transcription (ISO 639-1 code)
# Examples: "de" for German, "en" for English, "fr" for French
# Leave empty or remove for auto-detection
NEXT_PUBLIC_WHISPER_LANGUAGE=de